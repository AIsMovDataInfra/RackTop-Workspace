import { useState } from 'react'
import { api } from './api'
import { Dialog } from './Dialog'
import { errorText } from './errors'
import type { Resource, Translate } from './types'

export function ResourceDialog({ resource, onClose, onSaved, t }: { resource?: Resource; onClose: () => void; onSaved: () => void; t: Translate }) {
  const [name, setName] = useState(resource?.name || '')
  const [cluster, setCluster] = useState(resource?.cluster || '')
  const [gpuModel, setGpuModel] = useState(resource?.gpuModel || '')
  const [gpuCount, setGpuCount] = useState(String(resource?.gpuCount ?? 8))
  const [notes, setNotes] = useState(resource?.notes || '')
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState<unknown>(null)
  return <Dialog title={resource ? t('编辑资源', 'Edit resource') : t('添加资源', 'Add resource')} subtitle={t('维护团队可预约的服务器清单', 'Manage servers available for team reservations')} onClose={onClose} busy={busy} t={t}><form onSubmit={async (event) => { event.preventDefault(); if (busy) return; setError(null); try { const count = Number(gpuCount); if (!name.trim() || !cluster.trim()) throw new Error(t('请填写资源名称与所属集群。', 'Enter a resource name and cluster.')); if (!Number.isInteger(count) || count < 0 || count > 64) throw new Error(t('GPU 数量须为 0–64 的整数。', 'GPU count must be an integer between 0 and 64.')); if (count > 0 && !gpuModel.trim()) throw new Error(t('请填写 GPU 型号。', 'Enter the GPU model.')); setBusy(true); const draft = { name: name.trim(), cluster: cluster.trim(), gpuModel: count ? gpuModel.trim() : '', gpuCount: count, notes: notes.trim() }; if (resource) await api.updateResource(resource.id, draft); else await api.createResource(draft); onSaved() } catch (reason) { setError(reason) } finally { setBusy(false) } }}><div className="dialog-body"><div className="field-pair"><label>{t('资源名称', 'Resource name')}<input aria-label={t('资源名称', 'Resource name')} required maxLength={80} value={name} disabled={busy} placeholder={t('例如：训练服务器 01', 'For example: Training server 01')} onChange={(event) => setName(event.target.value)} /></label><label>{t('所属集群', 'Cluster')}<input aria-label={t('所属集群', 'Cluster')} required maxLength={80} value={cluster} disabled={busy} placeholder={t('例如：研发集群', 'For example: Research')} onChange={(event) => setCluster(event.target.value)} /></label></div><div className="field-pair"><label>{t('GPU 型号', 'GPU model')}<input aria-label={t('GPU 型号', 'GPU model')} maxLength={80} value={gpuModel} disabled={busy || Number(gpuCount) === 0} placeholder="NVIDIA A100" onChange={(event) => setGpuModel(event.target.value)} /></label><label>{t('GPU 数量', 'GPU count')}<input aria-label={t('GPU 数量', 'GPU count')} type="number" min={0} max={64} step={1} required value={gpuCount} disabled={busy} onChange={(event) => setGpuCount(event.target.value)} /></label></div><p className="field-help">{t('CPU 服务器填 0，仅支持整机预约。已有有效预约时不能修改 GPU 数量。', 'Enter 0 for a CPU server (whole-machine bookings only). GPU count cannot change while active reservations exist.')}</p><label>{t('使用说明（可选）', 'Usage notes (optional)')}<textarea aria-label={t('使用说明', 'Usage notes')} rows={3} maxLength={500} value={notes} disabled={busy} placeholder={t('例如：适合大模型训练；维护时间请提前告知', 'For example: Suitable for large-model training')} onChange={(event) => setNotes(event.target.value)} /></label><p className="field-help">{t('这里只维护资源名称和硬件信息，无需填写服务器密码、私钥或其他凭据。', 'This catalog stores names and hardware information. Server passwords, private keys and other credentials are not needed.')}</p>{error ? <div className="error" role="alert">{errorText(error, t)}</div> : null}</div><footer><button type="button" disabled={busy} onClick={onClose}>{t('取消', 'Cancel')}</button><button type="submit" className="primary" disabled={busy}>{busy ? t('保存中…', 'Saving…') : resource ? t('保存更改', 'Save changes') : t('添加资源', 'Add resource')}</button></footer></form></Dialog>
}

export function ResourceToggleDialog({ resource, onClose, onSaved, t }: { resource: Resource; onClose: () => void; onSaved: () => void; t: Translate }) {
  const [busy, setBusy] = useState(false)
  const [error, setError] = useState<unknown>(null)
  return <Dialog compact title={resource.enabled ? t('停用资源', 'Disable resource') : t('启用资源', 'Enable resource')} onClose={onClose} busy={busy} t={t}><div className="dialog-body"><strong>{resource.name}</strong><p>{resource.enabled ? t('停用后不能创建新预约，已有预约和历史记录将保留。请与受影响的预约人确认安排。', 'Disabling prevents new bookings and preserves existing reservations and history. Coordinate with affected members first.') : t('启用后，团队成员可以再次预约此资源。', 'Team members will be able to book this resource again.')}</p>{error ? <div className="error" role="alert">{errorText(error, t)}</div> : null}</div><footer><button disabled={busy} onClick={onClose}>{t('返回', 'Back')}</button><button className={resource.enabled ? 'danger' : 'primary'} disabled={busy} onClick={async () => { if (busy) return; setBusy(true); setError(null); try { await api.updateResource(resource.id, { enabled: !resource.enabled }); onSaved() } catch (reason) { setError(reason) } finally { setBusy(false) } }}>{busy ? t('保存中…', 'Saving…') : resource.enabled ? t('确认停用', 'Disable resource') : t('确认启用', 'Enable resource')}</button></footer></Dialog>
}
