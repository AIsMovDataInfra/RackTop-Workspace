# 2.3.1 资产云端备份验证

本轮补齐云服务器定时备份运维，不修改业务 API、数据库结构或标签界面。网页运行源码仍为 2.3.0 的 `9fd1204`，运维工具独立部署。桌面安装包、默认分支合并和 GitHub Release 不属于本次线上备份配置。

## 生产持久化确认

2026-09-11 核对 systemd 实际环境，`TEAM_DB_PATH=/var/lib/racktop-team/team.sqlite`；数据库位于网页 release 目录之外，文件为 `racktop-team:racktop-team`、0600。服务端资产、照片、编号和历史写入同库，使用 WAL、FULL 同步和事务，写入成功后才回复 API。

生产库 `integrity_check` 通过，`foreign_key_check` 为零。此次核对有 17 台设备、20 条设备修改记录，照片表为空，共 21 张表（含 SQLite 系统计数器）。未创建、修改或删除生产业务资料。

## 即时备份和实际恢复

已在 2026-09-11 18:34:18（UTC+8）创建一致性基线：

`/var/backups/racktop-team/manual/baseline-20260911T103418Z-ny50Yq/snapshot.sqlite`

目录 root:root / 0700，数据库 0600；同一受限目录保留服务配置和当前 release 标识。完整性、外键和数据表检查通过，数据库 SHA-256 为 `b6b187456315a9dc631424a03f5632ba92c122eb5270379b3c13516366d783fa`。

在云服务器 `/var/backups/racktop-team/restore-checks` 下新建受限临时目录，从基线复制恢复数据库，通过当前生产版本 `createTeamServer` 初始化，并关闭恢复实例；不启动监听、不运行通知。独立核对初始化前后全部表内容和 schema 摘要一致，17 台设备和20条历史均保留，备份原文件摘要不变。检查完清理恢复临时副本，未停止或覆盖生产库。

生产照片表为空，因此本次生产照片解码数量为 0；另用合成设备和真实 JPEG 验证恢复成功，损坏 JPEG 被检查器拒绝，不能把合成照片测试描述为生产照片恢复经历。

## 自动快照验证

自动备份专项 10/10（8 项新增、2 项既有）通过；团队后端全部 148/148 通过。覆盖在线已提交 WAL、中文、64 位整数、完整 BLOB、重复执行不覆盖、96/30/12 保留策略、未知文件保护、异常清单、损坏快照、源缺失、外键错误和磁盘空间不足。磁盘不足测试使用稀疏文件触发空间预检，不实际填满磁盘。六处版本字段和 `git diff --check` 通过。

生产部署于 **2026-09-11 18:37:54（UTC+8）** 完成：

- 工具：`/opt/racktop-team/backup-tools`，独立于网页 release，root 所有。
- 单元：`racktop-team-backup.service` / `racktop-team-backup.timer`，每 15 分钟，开机启用、补跑停机期间错过的任务，flock 防止并发。
- 路径：`/var/backups/racktop-team/automatic`，root:root / 0700，快照与清单 0600；以实际网页用户验证无法访问备份树。
- 首次服务运行成功，完整快照为 `snapshot-20260911T103754432Z-70631380-e3aa-4784-b610-1b88ec25824c`，数据库 SHA-256 为 `dc10575ae440a67d9100e3a8e120bf364dffeb4472583767d6d2593f409c35ec`，21 张表，保留 1 份、删除 0 份。

通过实际 systemd `ProtectSystem=strict` 环境读取在线 WAL 成功；源库只读，备份在独立副本上转换为 DELETE 日志模式，无需伴随 WAL/SHM 才能恢复。首次运行后执行只读 `--verify`，并用自动快照再次进行当前生产版本的独立恢复演练，全部数据与 schema 保留、备份原件不变。运行脚本和 service/timer 四个文件的远程 SHA-256 均与本机一致。

团队网页、共享中继和 Nginx 均 active；网页进程 PID 保持 54224、重启次数 0。公网 HTTPS 的 `/api/health`、`/healthz`、`/equipment` 为 200，匿名 `/api/equipment` 为 401，探测 `/backups/snapshot.sqlite` 为 404。未重启或恢复覆盖生产库。

已验证实际备份服务运行、定时器 enabled/active 与下次调度时间；长期持续运行、主机重启后补跑和跨月实际历史积累尚未观察，保留规则由自动化测试覆盖。本次未改变界面与运行依赖，未重复前端构建或桌面打包。

## 限制与独立副本

每 15 分钟定时快照不等于零数据损失，故障时只能恢复到最近成功快照；停机、磁盘满或任务失败会扩大恢复窗口。保留策略是最近 96 份、最近 30 个 UTC 备份日期各一份、最近 12 个 UTC 备份月份各一份的并集，历史从启用后逐步形成。

当前还未指定独立云存储或第二台服务器，异机备份尚未配置。数据库和同机快照仍共享服务器磁盘，不能声称已经具备异地容灾或保证绝对不会丢失。环境秘密、数据库和真实业务内容均不上传 GitHub。恢复步骤与日常检查见[运维说明](../team-web/deploy/BACKUPS.md)。
