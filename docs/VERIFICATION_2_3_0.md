# 2.3.0 标签导出验证

验证日期：2026-09-11（UTC+8）。本轮范围是团队网页设备标签导出；开发验证使用隔离本机数据库和明确的测试设备，上线后使用已有生产会话进行只读界面验收，没有修改线上业务记录。

## 实现

设备标签弹窗只保留一个「打印标签」原生下拉入口，选项为 PDF（已裁剪）和 PNG。两种格式共用完整标签 SVG，按宽 140mm、300 DPI 对应的像素数量栅格化，包含所有字段、边框与二维码。PDF 使用标签宽高创建单页，MediaBox 与 CropBox 一致，图片覆盖整个页面，并设置默认按实际尺寸打印；不经过浏览器的 A4 打印流程。PDF 生成库按需加载。

生成中禁用重复选择；失败显示可重试提示。后台会话刷新产生的新对象和翻译函数不取消内容相同的导出；标签内容变化或弹窗卸载后不再保存旧结果。二维码地址不因资产信息更新而改变。

## 自动化

| 检查 | 结果 |
| --- | --- |
| 团队后端 Node 测试 | 140/140 |
| 团队网页 Vitest | 138/138，19 个文件 |
| 桌面前端 Vitest | 334/334，71 个文件 |
| 团队网页 TypeScript / 生产构建 | 通过 |
| 桌面 TypeScript / 生产构建 | 通过；保留既有大文件体积提示 |
| 六处版本字段 / diff 空白检查 | 通过，统一为 2.3.0 |

标签相关三个文件共 29 项测试，包含原有二维码、XML 转义和长文本回归，以及格式选择、完整标签内容、二维码未就绪或失败、导出忙碌、失败重试、卸载/切换设备后晚返回、等价对象重新渲染等场景。使用真实 PDF 解析校验宽 140mm、高 52.89/168.39/310mm 的单页 MediaBox 和 CropBox。

## 浏览器与文件

实际在本机 Chromium 网页中打开设备详情和标签弹窗，检查中文浅色、英文深色大字体、中文跟随系统，以及 390×844 窄屏。打印入口完整可见，可用键盘访问；标签内容区域允许水平滚动。分别选择 PDF 与 PNG，观察生成状态恢复、错误日志为空。

为检查实际文件，测试专用 Vite 中间件保存浏览器传给下载函数的 Blob 副本。该中间件仅运行在本机验证环境，不属于提交的产品代码，也不用于线上环境。内嵌浏览器的下载完成事件未返回，因此这些检查证明文件生成和内容，未证明保存到系统下载目录的流程。

| 实际中文样例 | PDF 页面尺寸 | PNG 像素尺寸 |
| --- | --- | --- |
| 普通标签 | 140 × 52.8889mm，单页 | 1654 × 625 |
| 120 字设备名、各 80 字责任人和使用人 | 140 × 119.1944mm，单页 | 1654 × 1408 |

PDF 经独立解析和渲染检查，页面与裁剪边界一致，所有文字、四边框与二维码均完整，没有 A4 白边。英文样例的字段按完整单词换行。普通/长文本、中文/英文样例的 PNG 与 PDF 渲染图经二维码解码检查，指向对应的本机测试设备地址。

## 生产部署与数据保持

[在线设备管理](https://136.0.110.161/equipment)于 **2026-09-11 18:01:59（UTC+8）** 切换至 `/opt/racktop-team/releases/2.3.0-label-export-9fd1204`，部署源码为 `9fd1204eceb0c5d47865dcc2c725614cd87e2f08`。实际旧目录为 `2.2.0-web-network-ea7ce1d`；切换前核对线上源码，服务端与本轮源码逐文件一致。新版本保留旧的静态哈希文件供尚未刷新页面的客户端继续读取，完整部署新入口、样式及 PDF 按需加载文件。

新目录完成生产依赖安装与实际 sharp JPEG 编码检查后，短暂停止团队服务。通过 SQLite backup API 创建备份 `/var/lib/racktop-team/backups/label-deploy-20260911T100158Z-54169/before.sqlite`，目录 root:root / 0700，文件 0600；备份包含 WAL 已提交内容并通过完整性检查。初始化新服务时不启动监听和通知定时器，前后比较全部 20 张业务表及 1 张系统表的列、行数、逻辑内容摘要，59 个 schema 对象及数据库 pragma，结果完全一致。原环境文件与 systemd 单元摘要一致，生产库仍为 racktop-team:racktop-team / 0600。

切换后团队、共享中继和 Nginx 均为 active。公网 14 项 HTTP/TLS 检查通过：首页及四个业务页面、团队健康检查、中继健康检查、四个匿名业务接口拒绝访问，以及三份新静态资源。五个 HTML 响应与三份资源均与本轮构建的 SHA-256 一致；HTML 为 no-cache，JS/CSS 为 immutable，按需 PDF 文件返回正确 JavaScript 类型，CSP 允许 blob 图像。

生产浏览器沿用既有超级管理员会话，进入设备详情并打开标签，确认两个旧下载入口已删除，只有「打印标签」及 PDF（已裁剪）/PNG 选项；分别选择两种格式后，生成状态结束、控件恢复可用、没有导出错误。没有新增或编辑真实设备、账号或业务记录。文件内容和裁剪验证仍采用上方本机实际生成文件，浏览器保存到系统目录的完成事件未取得，不能视为已经覆盖所有下载和打印环境。

原 release 和一致性备份均保留。本轮不改变数据库结构，常规回退为恢复旧 current 链接并重启团队服务；不自动用旧快照覆盖上线后的正常业务写入。

## 未覆盖与交付状态

- 团队网页已部署；未发布 Git 标签、GitHub Release 或新桌面安装包，功能分支继承已完成的 2.2.2 兼容性工作。
- 本轮未修改 Rust 逻辑，未重新执行原生 Rust 测试或 Linux/Mac 安装包构建。
- 未验收实体打印机、手机相机扫码和 Safari/Firefox；未验证浏览器将文件实际保存到系统下载目录。
- PDF 的物理宽度明确为 140mm。PNG 提供对应 300 DPI 的像素数量，不写入打印分辨率元数据；图片打印软件的缩放需按目标标签宽度设置。
- npm 审计仍报告既有 Vitest 开发依赖的两项中等告警；新增 PDF 依赖未增加告警。本轮不升级无关测试工具。
