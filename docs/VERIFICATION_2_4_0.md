# 2.4.0 设备总体统计与新增资产备份验证

验证日期：2026-09-11（UTC+8）。分支为 `codex/v2.4.0-equipment-overview`。本轮新增团队网页设备统计，并为用户新登记的云端资产执行即时备份；不修改数据库结构或桌面功能。统计页面已于 20:03:06 部署上线。

## 实现与权限

设备目录显示总数和 available / in_use / maintenance / retired 四种状态数量，通过原生展开控件展示公司、类别、位置分布。统计独立于搜索及公司、状态筛选，打开目录、返回目录和主动刷新时读取，无定时自动刷新。加载、失败重试和会话变化均有相应状态处理。

`GET /api/equipment/stats` 在设备 UUID 路由之前匹配，拒绝查询参数；服务端在同一只读事务内聚合全部可见设备，不读取照片内容。成员及普通管理员仅本公司，超管包括未分配的历史设备。四种状态总是含零值，分组只返回实际存在的公司、类别及位置并保留历史值，不暴露设备名称、编号、人物或备注。没有新增图表依赖或数据库迁移。

## 自动化验证

| 检查 | 结果 |
| --- | --- |
| 团队后端 Node 测试 | 152/152 通过 |
| 团队网页 Vitest | 145/145 通过 |
| 团队网页 TypeScript 与生产构建 | 通过 |

共 297 项测试通过。新增覆盖空库、1005 台全量统计、状态及各维度合计、旧分类／位置／未分配公司、普通成员与资源管理员隔离、超管范围、设备转公司后更新、未登录／注销／未分配权限拒绝，以及查询参数、方法和路由顺序。前端检查包含总体统计独立于筛选、展开明细、空值、读取失败与重试。

构建产生 `index-Drxzp9Hj.css`、`index-DAtahjKQ.js`、`index-CwloiUfb.js`，入口、样式及按需 PDF 模块均已完整部署并核验。

## 实际浏览器验证

使用隔离的本机测试库验证，合成样例共 32 台，其中可用 18、使用中 8、维修中 4、已退役 2；这些分布不是生产资产统计。

- 中文浅色与跟随系统、普通及大字体、英文深色大字体显示正常。
- 390×844 窄屏无页面水平溢出。
- 原生分布展开控件可通过键盘 Enter 展开和收起。
- 选择维修中后，目录为 4 / 32，总体统计仍为 32。
- 本次浏览器错误日志为空。

界面沿用项目设计规范及已安装的 `emil-design-eng` 技能；开发技能目录被忽略，不进入运行依赖或发布包。

## 生产新增资产备份

2026-09-11 19:50:10（UTC+8）已执行即时一致性快照，目录为：

`/var/backups/racktop-team/automatic/snapshot-20260911T115010374Z-848f97ce-c5f2-4545-8785-2250079102d3`

快照数据库 SHA-256 为 `3cbd59e415981ffd7a23a9e2a807f2094672d2a1c56c2873731c9293df15379e`，包含当时全部 32 台设备、37 条修改记录、0 张照片及 21 张表。生产完整性与外键检查通过；照片表为空，不能把本次检查描述为实际生产照片恢复验证。

该快照已在独立副本上使用新版本完成恢复演练，32 台设备、37 条修改记录及 0 张照片保留。上线后于 20:03:53 再次执行自动备份，`snapshot-20260911T120353244Z-a6c7e4ed-edee-4359-bc54-5e5fe7c11699` 的数据库 SHA-256 为 `b4969d44cdc55a9bd64446bbde94c51e83a1318a92f89aa829ea4db7cf8f3896`，`--verify` 通过。

已观察到自动备份定时器于 20:00 实际运行，20:03 检查时共 10 份快照，下次计划为 20:15。既有每 15 分钟和近期／每日／每月保留策略继续适用；本轮即时备份未覆盖或回退生产数据。长期跨月保留和异机容灾不由此次短期检查证明。

## 生产部署

[在线设备管理](https://136.0.110.161/equipment)于 **2026-09-11 20:03:06（UTC+8）** 切换至 `/opt/racktop-team/releases/2.4.0-equipment-stats-647ab16`，运行源码为 `647ab162ade78c52ffbc8dd8813d9109087e1011`。部署包 SHA-256 为 `fb2435866506aee69c49784d6d220dfb8bfce6646ab5576a9897345ed81a9c7b`。

切换前创建 `/var/backups/racktop-team/manual/overview-deploy-20260911T120306Z-58461/before.sqlite`。新服务初始化前后全部 21 张表、59 个 schema 对象及内容摘要一致，服务环境、systemd 单元、备份工具和定时器摘要未改变。团队服务、共享中继、Nginx 及备份定时器均 active。公网 15/15 项检查通过，其中 8 个响应文件的内容摘要与构建一致。

生产浏览器沿用已登录的超级管理员会话，只读核对统计与独立 SQL 完全一致：共 32 台，全部为使用中且属于 A公司；显示屏 16、台式主机 10、机械臂 4、夹爪 2；上海 29、太仓 3。选择维修中后目录为 0 / 32，总体统计仍为 32；恢复全部状态并刷新后目录为 32 / 32。浏览器错误日志为空，未为验收新增或修改生产资产。

代码已通过组织 GitHub App 推送到 `origin/codex/v2.4.0-equipment-overview`，推送前隐私检查通过，共检查 3527 个对象。创建 Draft PR 返回 `Resource not accessible by integration (createPullRequest)`，因此尚未创建 PR；该权限限制未阻止已完成的网页部署。

## 未覆盖范围

- 本轮未运行桌面前端／Rust 测试、构建 Linux/Mac 安装包或发布 GitHub Release；改动范围为团队网页。
- 统计刷新由打开目录、返回目录或点击刷新触发，不承诺其他成员编辑后的自动实时更新。
- 独立云存储或第二台服务器仍未配置，同机快照不能保证整机／磁盘损坏时恢复，也不承诺零数据损失。
- 本轮未验收实体手机、Safari/Firefox 或实体打印机；既有标签功能的验证边界见 [2.3.0 记录](VERIFICATION_2_3_0.md)。
